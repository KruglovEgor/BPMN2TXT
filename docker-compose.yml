services:
  backend:
    build:
      context: ./services/pipeline
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ./services/pipeline:/app
    environment:
      - PYTHONUNBUFFERED=1
